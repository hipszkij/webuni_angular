import { FruitDirective } from './fruit.directive';

describe('FruitDirective', () => {
  it('should create an instance', () => {
    const directive = new FruitDirective();
    expect(directive).toBeTruthy();
  });
});
