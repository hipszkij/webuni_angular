import { ColorizedDataDirective } from './colorized-data.directive';

describe('ColorizedDataDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorizedDataDirective();
    expect(directive).toBeTruthy();
  });
});
