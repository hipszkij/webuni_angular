import { CoronavirusDataPipe } from './coronavirus-data.pipe';

describe('CoronavirusDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CoronavirusDataPipe();
    expect(pipe).toBeTruthy();
  });
});
